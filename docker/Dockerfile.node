FROM my-team-base:latest

# Install Node.js package managers
RUN npm install -g pnpm yarn && \
    curl -fsSL https://bun.sh/install | bash && \
    ln -s /root/.bun/bin/bun /usr/local/bin/bun

LABEL my-team.image-type="node"
